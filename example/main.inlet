mod bar {
    mod baz {
        fn foo(y: {*}) -> {example::bar::baz} {
            let x = (6 + 6) + y;
            return x;
        }
    }

    fn some(x: {*}, y: {boo::bar}) -> {*} {
        let z = example::bar::baz::foo(x);
        return x;
    }
}