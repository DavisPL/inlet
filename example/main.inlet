mod bar {
    mod baz {
        fn foo(y: {example::bar}) -> {boo::bar::baz} {
            let x = (6 + 6) + y;
        }
    }

    fn some(x: {*}, y: {boo::bar}) -> {*} {
        let z = example::bar::baz::foo(x, 5 + 6);
    }
}