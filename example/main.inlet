mod bar {
    mod baz {
        fn foo(y: {*}) -> {example::bar::baz} {
            let x = (6 + 6) + y;
            return example::bar::some(5, 10);
        }
    }

    fn some(x: {*}, y: {example::bar}) -> {example::bar::baz} {
        let z = example::bar::baz::foo(y);
        return z;
    }
}