mod bar {
    mod baz {
        fn foo(y: {*}) -> {example::bar::baz} {
            let x = (6 + 6) + y;
            return y;
        }
    }

    fn some(x: {*}, y: {boo::bar}) -> {*} {
        let z = example::bar::baz::foo(y, 6 * 7);
        return x;
    }
}