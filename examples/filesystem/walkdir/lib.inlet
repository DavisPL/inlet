// This is third-party code we got from the internet
fn walk() -> {*} {
    let raw = 123;
    
    // Try commenting this out and see what happens!
    // let result = std::fs::read(raw, 2);
    return 0;
}